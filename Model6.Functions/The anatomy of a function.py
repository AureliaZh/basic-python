def performOperation(num1, num2, operation='sum'): #parameters
    if operation == 'sum':
        return num1 + num2
    if operation == 'multiply':
        return num1 * num2
    
performOperation(2,3) #arguments
